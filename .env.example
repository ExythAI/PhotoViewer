# Environment variables for PhotoViewer
# Copy this to .env and fill in your credentials

# SMB/CIFS credentials for network share
SMB_SHARE=//winnfs/FamilyPhotos
SMB_USERNAME=your_username
SMB_PASSWORD=your_password

# Application port (default: 8080)
APP_PORT=8080

# Scanner interval in minutes (default: 60)
SCAN_INTERVAL=60

# JWT signing key (auto-generated by install script)
JWT_KEY=PhotoViewerSuperSecretDefaultKey2026
